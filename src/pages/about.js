import React from 'react'

import Layout from '../components/layout'
import SEO from '../components/seo'
import colors from '../helpers/colors'

const AboutPage = () => (
	<Layout color={colors.blue}>
		<SEO title='Home' />
	</Layout>
)

export default AboutPage
